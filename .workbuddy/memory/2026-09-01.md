# 2026-09-01

## eduevidence-bilibili 短视频大修（V1H/V1V，长视频 V2 明确搁置）

### 根本性纠错：视频介绍对象 = EduEvidence Skill（不是独立产品）
- 介绍的是 `D:/motion/projects/eduevidence/` 的 **SKILL.md（EduEvidence 6.0）**：9 步协议
  （Frame→Retrieve→Extract→Challenge→Audit→Adjudicate + Applicability→Intervene→Evaluate，
  Present 是投影层）、6 道科学门、四态判决 ADOPT/PILOT/DO_NOT_ADOPT/INSUFFICIENT
- **8 角色不是固定接力**（用户重点纠正）：`integrations/agent_mcp.py` ROLE_REQUIREMENTS 只定义
  能力需求（推理/速度/成本/结构化输出/上下文/工具），具体模型来自用户批准的 CLI 池
  （opencode/omp/codex/grok…）；**skeptic 必须异构模型家族**（different-model-family）；
  Cross-Model Review 独立复核。国产底座识别：kimi/moonshot、deepseek、glm(智谱)、qwen、mimo(MiniMax)
- 旧 vo-v1-03 文案编造型号（GPT 5.6 SOL / Kimi K3 / DeepSeek V4 Pro）已重写

### V1V 崩溃根因（React #130 frame 3615）
- `v1mech.tsx` import 了 `mechanism.tsx` **不存在的导出 `CrossPing`** → undefined → McpSVertical
  渲染即崩；V1H 走横版分支不渲染所以没崩。已实现 CrossPing（两点往返挑刺/回击）修复

### UI bug 根因清单（截图诊断确认）
1. **节点主标签不可见**：AgentS 把便签底色(noteY/noteG/noteB/noteR 近白)当主题色传 MechNode
   → 白字白框。修复：MechNode 主标签固定深墨（labelColor 可覆盖），主题色只上边框/光圈/角标
2. **ghost 语义写反**：`if (a0<=0 && ghost) return null` 导致未 trace 节点完全不渲染 → 长时间空屏。
   修复：preview=true 默认显示灰框预览（拓扑全貌），到达时点亮
3. **MechToken 终点常驻**：TokenCard 盖住判决书节点。修复：终点脉冲后淡出（证据"交付"语义）
4. **MechEdge SVG viewBox 写死 2000×1100**：竖版裁切风险 → 加 vw/vh 参数
5. **McpS flex 布局 + 硬编码线坐标错位**（4 卡 3 线、线打空白处）→ 全绝对坐标重排
6. **MechNode 字号写死 24px**：大节点字太小 → 随 size 缩放（0.22 系数 clamp）
7. 判决书场景缺"判决落下"动画 → 补（at 105 从横梁落到四态卡下方，两段弹跳）
8. 判决书卡 260 宽文字折行 → 370 宽 nowrap + dropY 828
9. 底部口号/说明与进度猫轨道重叠 → 移位（AgentS 左下 bottom74、McpS bottom92）
10. AgentS 横版用了 anim(f) 但没声明 const f = useF() → 运行时 ReferenceError（截图时发现已修）

### 布局重做（对齐 diagram-design skill 原则）
- 彩色是编辑性的：激活才彩色，未激活中性灰；每条连接承载信息；泳道与节点严格对齐
- FlowS：三泳道(235/735/1230) 严格对齐 9 节点；trace 间隔 30→18（6.3s 走完，余量展示全貌）
- AgentS：删大泳道框，改节点色分组+右上角图例；sub 改能力需求（推理优先/快·强工具调用/
  必须异构模型家族…）；底部真实模型池条（Claude·GPT·DeepSeek·GLM·Kimi K2 Code·Qwen·MiniMax，
  国产红框高亮）；标题改"8 个角色 · 一种用法组合"
- McpS：角色层(4) → Agent MCP 协议桥 → 底座层(Claude Code/Codex CLI + 国产池 Kimi K2 Code/
  DeepSeek/GLM/Qwen·MiniMax 红虚线分组框)；CrossPing 在 Kimi↔DeepSeek 间往返
- ArchS（v1short.tsx）：4 列平铺 → 纵向 layer-stack 四条带（宿主接入/证据引擎核心[红焦点]/
  角色协作[按能力路由]/报告产出），层间 ▼，删"222 主张·118 研究"案例噪音
- 竖版 4 机制场景全部独立排版铺满 1080×1920（不再横版缩放）

### 文案改动（2026-09-01）
- pronunciations.json 两段重写（备份 .bak-0901）：
  - vo-v1-03：角色按能力路由+怀疑者异构家族+国产底座 Kimi K二 Code/DeepSeek/GLM/Qwen+一种用法组合
    第一版 17.12s 超窗（挂载 33.52+17.12=50.64 压到下段口播 50.96），精简重录后 **14.90s** ✓
  - vo-v1-arch：四层去"八智能体协作"→"角色协作"，删 archify 表述，**8.94s** ✓
  - original 字段同步更新为可读版（Kimi K2 Code/GLM 正常写法，字幕用）
- **TTS 陷阱**：regen_only.py 必须用 `.venv/Scripts/python.exe`（系统 Python310 无 cosyvoice 依赖，
  报 ModuleNotFoundError: hydra）；v3 已带 speed 1.35 + endofprompt 前缀
- durations.json 更新 v1-03: 14.9 / v1-arch: 8.94
- **V1 时间轴硬编码在 Root.tsx（不读 durations.json）**：新 vo ≤15s（vo-v1-03）/≤9.9s（arch）即无需重排 ✓

### 硬字幕烧进（用户要求：字幕直接嵌进视频 + GPU 渲染）
- 新组件 `engine/src/kit/subtitle.tsx` SubtitleTrack：手帐风白底(0.96)+#D8D0BC 边+圆角 14，
  横版 33px/bottom34、竖版 36px/bottom116、maxWidth 90%（40px 会把"核心"拆行——已降 36）
- cue 生成 `tools/voice/cosyvoice/make_v1_subs.py`：挂载点+durations 按字符比例切分句读，
  MAXLEN 22 → 输出 `engine/src/subs_v1.ts`（37 条 cue，末条 110.48s < Logo 111.96 ✓）
- Root.tsx：VideoBody 加 subs prop（绝对时间轴，ProgressCat 后、zIndex 60），仅 V1H/V1V 传入
- 渲染 `engine/render_v1.mjs`：只渲 V1H/V1V（V2 搁置），hardwareAcceleration if-possible(NVENC)+concurrency4

## 09-01 上午第二轮：真实模型池 + 国产底座 + 8 格图表 + 出入动画 + 去运镜

### 用户原话关键纠正
- 之前以为"GPT 5.6 Sol / Kimi K3 / DeepSeek V4 Pro"是编造型号 → **全是真实**（用户生态 2026 年型号）
- 真实体系（用户指定）：
  - 高级模型（strong/推理裁决）：Claude Opus 5 / GPT 5.6 Sol / DeepSeek V4 Pro / Kimi K3 / GLM 5.3
  - 任务模型（fast/并发检索）：Claude Sonnet 5 / GPT 5.6 Luna / DeepSeek V4 Flash / GLM 5.3 Flash
  - 底座（CLI）：Claude Code / Codex / DSH / Kimi Code / ZCode / OMP
- 国产底座：DSH / Kimi Code / ZCode（红虚线分组框 · 本地可用）；通用：Claude Code / Codex / OMP

### 修复（v1mech.tsx + Root.tsx + v1short.tsx）
1. **ModelPoolBar → ModelPoolBars 两级池**（高级红 + 任务蓝），紧凑 2 行
2. **AgentS 横版**：判决书节点 y 912→858；ModelPoolBars y 950 compact
3. **McpS 底座名单全替换**（横版 6 卡 + 国产分组框 + 底座层标签"Claude Code/Codex/OMP 通用｜ DSH/Kimi Code/ZCode 国产"；
   竖版去框改红边 + 底部说明加"DSH·Kimi Code·ZCode 国产底座"）；CrossPing DSH↔Kimi Code
4. **Root.tsx 转场自然化**：TRANS 24→30 帧（0.5s）、springTiming damping 200→32（轻弹，stiffness 220, mass 0.9）
5. **ReportCardsS 5 卡居中加大**（flex+gap 等距、±6° 微旋转、卡宽 300/高 430；竖版 208×330）
6. **图表一屏 → 8 格迷你图网格**（柱/森林/分布/折线/饼/桑基/散点/雷达，每格示意数据 + 小标题）
8. **每格出入动画 + 两波轮播**：波 1 at 66+i*8 入 / outAt 152+i*5 出；波 2 at 170+i*8 / outAt 252+i*5
9. **图表署名别人的 skill**："charts by ECharts · trace by archify"

### TTS + 字幕
- vo-v1-03 第一次 19.48s 超窗 → 精简 + speed 1.5 → **12.28s** ✓（手动 regen_v103.py + ffmpeg 同步）
- vo-v1-05 重录 8.10s ✓
- durations.json 更新 v1-03: 12.28 / v1-05: 8.10
- make_v1_subs.py 更新 + 重新生成 subs_v1.ts（38 cues）
- **Windows 路径**：ffmpeg 必须用 `D:/...`（Git Bash `/d/...` 不识别）
- **regen_only.py 必须 .venv/Scripts/python.exe**（系统 Python310 缺 hydra）

### CINE 方案（src/scenes/cinematic/ + src/video/）—— 用户并行推进的另一条线
- V1H-CINE/V1V-CINE 用 MechanismPreWeb/MechanismPostWeb（v3 mechanism 组件体系，独立实现）
- CinematicV1 用 src/video/narration.ts + SubtitleLayer（与 subs_v1.ts 不同）
- 这次没动 CINE（用户最近反馈针对 V1H/V1V VideoBody 版）

### 验证工具
- `engine/shot3.mjs`（11 帧全场景）+ `shot4.mjs`（4 帧回归）+ `shot5.mjs`（字幕 2 帧）
  renderStill 截帧自检流程可复用

## 09-01 中午微调：McpS+WebTourS+开口播

### McpS（v1mech.tsx）
- 国产分组框（DSH/Kimi Code/ZCode）**去掉"国产底座·本地可用"文案**，框保留
- CrossPing ay/by 744→768 + curve 64→**-52**（下凸弧避免被底座卡 zIndex 4 遮挡）
- 怀疑者"必须换一个模型家族"→"**最好**换一个模型家族"（口播 + 底部说明 + AgentS skeptic cap 三处）
- 竖版 CrossPing 1360→1382, curve 70→-58

### WebTourS（v1short.tsx）
- 滚动更快完成：scrollP anim 时长 400→**180** 帧（7s 滚完，留 ~2.5s 展示控制台）
- 底部标签"5 主题 · 滚动浏览全部证据"→"**本地控制台 · 随时进入（独立本地服务）**"

### 开头文案（对齐 SKILL.md 6.0：education + applied social science + AI tool/program/policy）
- vo-v1-01 旧"循证教育决策引擎…教育问题" → 新"**决策级证据引擎**…教学法、课程、AI 工具还是政策，都能先摆证据、再下判决"
- 9.94s ✓（IntroS 场景 0-10.98）

### vo-v1-07 本地控制台
- 旧"滚到底进入控制台" → 新"**还有本地控制台，随时进入**"
- 8.02s ✓

### 流程教训
- 用户原话中的真实生态型号（GPT 5.6 Sol / Kimi K3 等）**不是编造**——是我之前的认知错误
- 怀疑者"必须"→"最好"是软化硬性约束，与 SKILL.md "should" 的 advisory 语义一致
- "滚到底进入控制台" → "本地控制台·随时进入·独立本地服务"——后者更准确（控制台是独立 Web Studio 服务）

## 09-01 下午：残留旧名单清理 + FlowS 九步旁白对齐

### 全面扫描结果（grep src/）
- ❌ **v1short.tsx:60 ArchS 角色协作层残留旧名单** "Kimi K2 Code · DeepSeek · GLM · Qwen · Claude · GPT"
  → 改为真实两级池（Opus 5 / GPT 5.6 Sol / DS V4 Pro / Kimi K3 / GLM 5.3 / Sonnet 5 / Luna / DS V4 Flash / GLM Flash）
- ❌ v1mech.tsx:16 注释 "Kimi K2 Code" 残留 → 改为 DSH / Kimi Code / ZCode
- 其它残留（V2 agent.tsx/handdraw.tsx、v1short 158-161 未使用的 McpS）暂不动——V2 搁置

### FlowS 九步旁白与动画对齐
- vo-v1-02 挂 21.74s / 10.28s："定框、检索、抽取、挑战、审计、裁决，再到试点与评估。"
- 按字符权重计算每步时刻 → 30fps 基准帧
- 新 TRACE = [8, 74, 91, 107, 124, 141, 158, 172, 186, 203, 239]
  （头 8 → 定框74 → 抽取107 → 审计141 → 裁决158 → 适用172 → 试点186 → 评估203 → 判决书239）
- 横竖版 FlowS 都用同一 TRACE（共享 VO）
- 验证：26.6s（旁白"裁决"瞬间）截帧 → 第 6 节点"裁决"刚好亮起，前 5 步已亮，后 3 步灰框预览 ✓
- `engine/shot3.mjs`（11 帧全场景）+ `shot4.mjs`（4 帧回归）+ `shot5.mjs`（字幕 2 帧）
  renderStill 截帧自检流程可复用
# 2026-09-01 — EduEvidence 6.0 Bilibili V1 短片(三版本统一机制动画)

## 交付产物(写入仓库零覆盖)
- `engine/out/eduevidence-v1h-cine.mp4`  114.41s  1920×1080@60  H.264+AAC  36.7MB
- `engine/out/eduevidence-v1v-cine.mp4`  114.41s  1080×1920@60  H.264+AAC  36.4MB
旧文件 v1h/v1v/v2/bilibili-main 全部完好(零字节改动)

## 方案来源
`C:/Users/10777/Downloads/EduEvidence_6_B站三版本_最终执行方案.md`(3864 行)
- 11 个分镜 S1-S11,固定时间轴 114.36s
- 4 个 Hero 动画:Claim Split / Scientific Audit / Decision Boundary / Empirical Re-Injection
- 硬规则:Speed=1.2 固定(禁止 1.3/1.35/1.4);WebTour 83.08-92.66s 冻结;横竖版独立关键帧
- 9 个科学硬门(§78):不能捏造 CI/SE/p-value;反证本身不涂红;决策不强行改判

## 真实数据锚点(全部来自产品仓库 commit 36405e4)
- E-001 KAZEMITABAAR 2023 N=69 K12 completion_time support
- E-004 BASTANI 2025 N=950 high school math 独立考试 -17% contradict
- E-005 护栏设计(hints not answers)support
- E-006 BASTANI 2025 任务表现 +48% support
- 判决 PILOT / confidence Moderate(0.612)

## 新建工程结构
- `engine/video/facts.ts` 单一事实源(7 findings / 3 sources / claim / gap / decision)
- `engine/video/storyboard.json` 11 场景 × V1/V2 时间
- `engine/video/layouts.ts` 横竖版独立布局(1400×3000 vs 3200×1800 世界)
- `engine/video/narration.ts` VO 挂载点(0.25s 前导)
- `engine/video/subtitles.ts` 字幕(10 句,CameraRig 外)
- `engine/src/kit/camera/cameraMath.ts` 关键帧采样 + parallax
- `engine/src/kit/camera/CameraRig.tsx` + ParallaxLayer
- `engine/src/kit/camera/cameraPlans.ts` V1H/V1V 独立关键帧(非 0.6 倍)
- `engine/src/kit/SubtitleLayer.tsx` 方案 §66 字幕层
- `engine/src/kit/mechanism/v3/*` 13 个机制组件:
  SemanticEdge+PathToken / EvidencePacket / ClaimNode / AdoptPull
  ClaimSplitter / ScientificGate / DecisionBoundary / RevisionLayer+RevisionMorph
  WebToFindingMorph / ResearchIntentNode / SourceShell+QueryRay
  StudyExploder / KnowledgeGapExtractor+PilotBlueprint
  ProjectionSpace+FinalLoop
- `engine/src/scenes/cinematic/MechanismPreWeb.tsx`(S1-S7 连续机制世界)
- `engine/src/scenes/cinematic/MechanismPostWeb.tsx`(S9-S10)

## TTS 流水线(方案 §82 硬门)
- `tools/voice/cosyvoice/segments_v1short.txt` 9 段中文旁白
- `tools/voice/cosyvoice/pronunciations-v1short.json` 发音规范化
- `tools/voice/cosyvoice/regen_v1short.py` Speed=1.2 硬编码,9 段实际 7-13s
- Audio Timing Guard 全部通过:graph_decision margin 0.81s(≥0.8 hero 要求)
- `durations.json` 从 27 段扩展到 40 段(vo-s* + vo-m* + vo-w* + vo-v1-* + 9 vo-c*)
- VO 挂载点:scene_start + 0.25s(WebTour 冻结段保持 83.48s)

## 渲染期间踩到的坑(避免复发)
1. **`L_BODY_FIELD is not defined`**:MechanismPreWeb.tsx 导入漏写,AuditBeat 引用报错
2. **`f is not defined`**:ProofBeat 组件 body 用到 f 但未 `const f = useF()`,rendering 阶段才暴露
3. **所有 beat 一起渲染导致画面错乱**:各 beat 必须有 `if (f < B[i].start || f >= B[i].end) return null` 早退,否则 Graph/PILOT 等后期元素在 S1 Hook 阶段就显示
4. **EvidencePacket 双重偏移**:外层 `position: absolute, left: x-130, top: y-34` + 内部 `left: x-width/2` 导致位置错乱,EvidencePacket 自带 absolute 定位,只包裹 opacity/transform 样式

## 未来工作
- V2 长版(344.2s)按方案 §32 重新规划,沿用 v1short 现有 Hook/Pain/Case/Outcome/Verdict 等组件做 LongPreWeb/LongPostWeb
- 字幕/贴纸/Pronunciations 在 V1 短片中验证生效,V2 直接复用
- storyboard.json 已含 V2 时间,LongPreWeb 沿用 S1-S7 节奏但每段更长

## 09-01 傍晚：IntroS 画面文案同步 + 响度统一 + B 站章节

### IntroS 画面文案同步（只换了旁白没换画面——用户指正）
- Kicker："循证教育决策引擎" → "决策级证据引擎"
- 主卡："你问一个教育问题，它先查证据，再给你判决书" → "问教学法、课程、AI 工具还是政策，它都先查证据、再给判决书"
- CTAS 尾部（顺带）："输入你的教育问题即可" → "输入你的决策问题即可"

### 响度统一（用户反馈声音大小不一致）
- 根因：今天新录 5 段（v1-01/arch/03/05/07）mean -25~-27dB vs 早期 5 段（v1-02/04/06/08/09）mean -16~-17dB——差 10dB（约 3 倍音量）
- 修法：成片音轨**分段增益 + alimiter 限幅**（ffmpeg volume:enable='between(t,..)' 串联，视频流 copy 不重编码）：
  轻段 +6.4~+8.5dB，重段 -1.1~-2.3dB，target ≈ -18.5dB，alimiter=limit=0.9 防削波
- 结果：十段全部收敛 mean -20.5~-20.9dB（极差 0.4dB），整片 mean -21.4 max -0.1 无削波
- 时长不变 111.018s，视频流 h264 原样 copy

### B 站章节（HH:MM:SS，间隔均 ≥5s）
00:00 开场 / 00:10 内部四层 / 00:21 九步证据流 / 00:33 角色×模型池 / 00:50 四态判决 /
01:00 Agent MCP / 01:11 真实案例 / 01:23 展示页+本地控制台 / 01:32 五种风格+8格图表 /
01:42 开源安装 / 01:52 片尾

### Windows 文件锁坑（复发）
- Remotion 渲染最后 rename 临时文件 → 目标被预览播放器锁住 → EPERM: operation not permitted
- 处理：V1H 100% 完成只差 rename，手动 mv 临时文件恢复；V1V 改渲染到 -new.mp4 再改名
- 教训：渲染前先确认 out/*.mp4 未被播放器占用；或输出到新文件名再手动覆盖

## 09-03：经验沉淀为项目级 skill
- 新建 `D:/motion/.workbuddy/skills/eduevidence-video-loop/SKILL.md`（agent_created: true）+ 同步用户级 ~/.workbuddy/skills/
- 内容（8 节）：语义权威源（SKILL.md 6.0 + agent_mcp.py；真实模型池两级名单；9 步协议；角色=执行适配器；skeptic advisory"最好"）、
  机制动画 v3 原则（灰框预览/深墨文字/彩色是编辑性/vw-vh 参数化/token 淡出/旁白字符权重对齐/竖屏独立排版）、
  修复自检 shot*.mjs 循环、CosyVoice3 TTS + 硬字幕流程（.venv、pronunciations 双字段、make_v1_subs.py、Root 时间轴硬编码）、
  响度统一（volumedetect + 分段 gain + alimiter）、NVENC 渲染与 Windows 文件锁 EPERM、
  VideoBody/CINE 双方案并存、交付与 B 站章节
- 动机：用户把 09-01 一整天 V1H/V1V 修复的成功经验要求固化，防四类最常见返工
  （模型名单编造、画面/旁白/字幕三处不同步、竖屏缩放、响度不统一）
