# 2026-08-29

## 将 Motion 项目的 29 个 skill 同步安装到 WorkBuddy

**背景**：项目 `skills.lock.json` 记录了 25 个 skill（Claude Code 侧），WorkBuddy 的 `~/.workbuddy/skills/` 为空。

**做了什么**
1. 盘点双源：项目内 `.agents/skills/`（19 个）+ 本地 `~/.claude/skills/` hyperframes 系列（10 个）。
2. 用锁文件 SHA-256 校验完整性，发现 8 个漂移 → 定位为 hyperframes CLI 由 v0.8.10 升级到 v0.8.14 导致的上游文档演进（非篡改）。
3. 安全审计 28 个源目录：6 处正则命中全部为误报（含 1 处防御性代码注释、1 处错误提示文案、1 处 .cn 官网链接）。
4. 发现并解决同名冲突：项目内编排 skill 与 HeyGen 官方原子 skill 都叫 `hyperframes-media`。经用户确认，**官方版保留原名**（被 6 处引用），项目版改名 `motion-media-handoff`。
5. 安装 29 个 skill 到 `~/.workbuddy/skills/`（99MB），修复 `motion-media-handoff` 的 frontmatter name。
6. 回写 `skills.lock.json`：刷新 8 个 digest、补录 4 个条目、记录 digest 算法。端到端校验 29/29 PASS。

**产物**
- `~/.workbuddy/skills/` 29 个 skill（16 upstream remote + 10 本地 hyperframes 系列 + 3 个项目内编排）
- `D:/motion/skills.lock.json` 更新至 29 条，备份在 `.workbuddy/tmp/skills.lock.json.bak`
- 验证脚本：`.workbuddy/tmp/{check2,probe_tree,update_lock,finalize_and_verify}.py`

**待办/未决**
- `source_tree_digest` 为 CLI 未公开算法，试了 4 种算法均未复现；漂移项的该字段已标记 `source_tree_digest_stale: true`，**未更新**。
- 项目最高优先级仍是 CLAUDE.md 第 0 条：AI 新闻内容侧（搜索选题 + 视频内容生成）大改重构。

## AI 新闻四方向重构计划——多维度审查（未执行，仅审查）

**背景**：用户要求先对 `ai-news-pipeline-overhaul_381833fe.md` 做多维度审查并优化计划，未让执行。

**做了什么**
1. 核验计划代码锚点（types.ts / gen-hyperframes.ts / daily-research.ts / generate-content.ts / news-sources.json / SKILL.md / package.schema.json），发现 3 处与现状矛盾：①命令链坏点在 SKILL.md 文档（package.json 命令其实对的）；②ResearchStream 是「三值→五值」且 world→intl 改名（非计划说的"扩四值"）；③机器之心移除是重复描述。
2. 用户拍板 3 决策：方案 A（intl 取代 world）、主链+并行支线、先探环境。
3. 联网查证搜索框架全部外部依赖免费方案（见 §7.5）：
   - X 官方 API 2026-02 后免费层已死、按量付费 → 不建议配；开源方案 x-tweet-fetcher（FxTwitter 免登录 + x_discover DuckDuckGo 免 key）更优。
   - GitHub PAT 免费、未认证 60次/时→认证 5000次/时，应从可选升必配。
   - TMDB 免费无付费档，娱乐方向必配。
   - 60s API（60s.viki.moe）免费免部署，/v2/ai-news 可额外喂 AI 方向。
4. 补充四方向设计语言表（ai=蓝紫/国际=灰蓝/国内=红/娱乐=橙粉）。

**产物**
- `D:\motion\.qoder\specs\ai-news-pipeline-overhaul_review.md`（审查附件）
- `D:\motion\producer\.env.example` 追加免费 key 配置清单（GITHUB_TOKEN/TMDB_API_KEY/X_AUTH_TOKEN）

**关键结论（用户需知）**
- Qwen3-Embedding-4B 已下载到 `models/Qwen3-Embedding-4B`（~8GB，sentence-transformers 兼容），Phase 4 embed-server 可直接加载。
- 待确认：§3.2 契约向后兼容策略（保留 world-news 标 deprecated + 新增 intl-news，vs 硬删除）。

## 用户对齐后的方向修正（重要，纠正我之前两处越界）

1. **设计风格**：AI 新闻方向用户已定 `claude`=暖米白 / `dark`=黑底青绿，早晚双场次。我擅自自创四方向配色是越界，已撤回。其他三方向（国际/国内/娱乐）由我设计，**每天各产一个**。
2. **X 定位**：X 是**一手信息源**（厂商官号/员工最先在 X 首发官方动作），与 RSS/GitHub 平级且优先级更高，不是「补漏工具」。抓取逻辑 = 每日轮询固定厂商官方账号白名单，非盲抓。
3. **X 技术选型定稿**：首选 `twscrape`（v0.20.0 2026-08-07 发布，cookie 认证走 GraphQL，`api.user_tweets` 直接拉账号 timeline）；备援 `Scweet`。已死勿用 snscrape/Twint/Nitter。
4. **白名单已落盘** `producer/config/x-watchlist.json`（30 账号：14 官方 + 16 员工/研究员，handle 多源交叉核对去 OCR 乱码，上线前需 twscrape 实测验证）。

**产物新增**
- `producer/config/x-watchlist.json`（X 白名单，46 账号：28 官方 + 18 员工/研究员，含 verify 字段标注需实测核验的 handle；关键新增 @deepseek_ai 国产官方 + Midjourney/Runway/Stability/Suno 等生成能力官号）

**X 层落地前提（已对齐）**
- 本机能访问 x.com 且有 auth_token cookie（用户确认）。
- 本轮策略：X 层先备结构（白名单已完成 + twscrape 采集代码结构 + 降级链），非 X 源主链优先跑通。

## Skill 布局补充：项目级副本（对上文迁移的增量）

用户确认后，将 3 个项目内编排 skill 复制到项目级 `D:/motion/.workbuddy/skills/`：
`motion-design`、`video-agency-roles`、`motion-media-handoff`。

**是复制不是移动**——用户级 `~/.workbuddy/skills/` 三份全部保留（已验证）。
校验 3/3 frontmatter 合规（name 与目录名一致、description 存在），且与用户级字节一致。

理由：这 3 个 skill 正文强绑定 `runs/<date>/<run>/` 等项目路径，放在项目级可随仓库走；
其余 26 个为通用上游能力，留在用户级跨会话复用。

**风险提醒**：两份副本无自动同步机制，改动任一份需手动同步另一份，否则内容分叉。

## ⚠️ 严重事故：git rm 触发 52 个文件被误删（已恢复，需规避）

**现象**：执行 `git rm producer/src/data/today.ai-news.json producer/src/data/today.openbot.json`
（仅 2 个旧 fixture）后，`git status` 显示 **52 个文件被删除**——
`producer/src/`（整个目录！）、`producer/docs/`、`producer/hyperframes/` 全部从工作区消失。
`ls /d/motion/producer/` 确认 `src/` 目录真实不存在。

**恢复**：所有文件均被 git 跟踪 → `git checkout -- producer/src/ producer/docs/ producer/hyperframes/`
一次性恢复成功，`npm run verify` 通过。
**代价**：`types.ts` 回退到 HEAD，Phase 0.1 契约改动丢失，已重新应用。

**教训（务必遵守）**：
1. **不要用 `git rm` 删文件**——改用 `rm` 后让 git 自然显示删除，或直接保留文件。
2. 删除类操作前先 `git status` 确认影响面；操作后**立即** `git status` 复核，发现异常马上 `git checkout --` 回滚。
3. 重要改动（如 types.ts 契约）**改完立即 commit**，否则一次文件系统事故就会丢失全部未提交成果。
4. `projects/eduevidence-bilibili`（10437 文件）未受波及，已验证完好。

## Phase 0 执行进度（Agent 模式）

已完成：
- **0.1 契约决策**：`types.ts` ContentType + StreamId 扩四方向（intl/cn/ent），保留 `world-news` 标 deprecated；
  `contracts/package.schema.json` stream enum 同步扩值 + 加 description 说明向后兼容。
- **0.3 验证脚本**：新建 `producer/scripts/run-verification.mjs`（node 直跑、ASCII 标记、tsc + node --test），
  package.json 加 `"verify"`；实测 `[PASS] tsc --noEmit` / `[SKIP] node --test`。
- **0.4 generate-content 三处**：NEWS_SECTIONS 扩四方向 + 新增 STREAM_LABEL（中文名）；
  system prompt section 说明；section 默认值改 `NEWS_SECTIONS[stream]?.[0]`；
  标题/章节用 STREAM_LABEL 且**仅 ai-news 带早晚场次后缀**（其余方向每天单场）。
- **0.5 npm install undici**：完成（6 packages，0 vulnerabilities）。
- **0.6 清理旧 fixture**：删除 `today.ai-news.json` + `today.openbot.json`（旧 fixture，git 可回滚）。
  **保留 `today.json`**——`publish.ts` L166 与 `style-check.ts` L15 明确依赖它，不是 fixture。

**Phase 0 已全部完成并提交**（commit d0bc911 + 060b74c），`npm run verify` 通过。

## Phase 1 完成（commit e7824d5）

- **5 本研究手册**落盘 `producer/config/research-handbooks/`：
  `ai-news` / `intl-news` / `cn-news` / `ent-news` / `bilibili-verification`。
  统一 frontmatter（version/updated_at/status/proxy_required/blindtest），
  正文固定五节：信源表 → 检索式与过滤器 → 降级链 → 打分权重 → 盲测记录表。
- **5 份提示词模板**落盘 `producer/config/prompt-templates/`：
  `host.v1.md` + `{ai,intl,cn,ent}-subagent.v1.md`。占位符保留，密钥不落盘。
- **SKILL.md 重写为四方向架构**：配额场次表、执行主体（agent 即 LLM，不调 API）、
  完整流程 9 步、四方向 section 契约表、配速与时长门、TTS 语气按方向。
- **关键认知修正**：generate-content 定位为「校验+组装 agent 产出的 blocks」，
  LLM 调用仅 degraded 兜底；提示词全文不进 SKILL.md（细节放模板）。

## Phase 2 部分完成（commit 2 个）

- **2.1 types.ts**：VideoBlock 加 `tag`（方向徽章）/ `media`（素材图）/ `subtitle`（内嵌字幕条）；
  section 联合类型追加 intl-news / cn-news / ent-news。
- **2.2 gen-hyperframes.ts**：
  - VideoBlockLite **双写** tag/media/subtitle（渲染端读此接口，漏写则读不到）
  - newsTotal 由硬编码 `ai-news/other-news` 改为**按 config.blocks 动态统计**，
    修复新方向进度显示 `n / 0`
  - **素材图本地化**：`media.src`（run 相对路径）→ 复制 `<outDir>/assets/media/<idx>-<file>`，
    basename 防路径穿越 + 规避 Windows 长路径；绝对路径拒绝并告警，缺失降级为无素材页
- **2.5 review-video.ts**：render 检查清单追加 4 条（Tab 高亮一致性/字幕条遮挡/素材图等比/四宫格不断行）

**冒烟验证通过**：四方向测试 run 生成 4 页合成（38.6s），
`assets/media/1-intl-shot.png` 落位，进度显示 `1 / 1`（修复前为 `n / 0`）。

## Phase 3 部分完成（采集与打分全链路已跑通）

- **3.5 部分公共库**：新增 `src/lib/streams.ts` 单一真相源（ResearchStream 六值、NEWS_STREAMS、
  STREAM_LABEL、streamToCategory、isResearchStream），daily-research/score-and-rank 统一导入。
- **3.2 daily-research.ts**：白名单改 ALL_STREAMS（四方向通过）；category 映射改函数。
- **3.4 score-and-rank.ts**：
  - `--fallback-only`：degraded 兜底，按分数取 top 产统一 selection.json
    （selected_by="score-fallback"，复用 --selection 消费链，不开第二输入分支）✓ 实测
  - `--limit` 可配额取条数
  - **sourceQuality 配置驱动化**（读 news-sources.json trust_level）——修复新增信源
    不在硬编码集合内导致 26 条全部 3.05 分无区分度；修后 high=4.4/medium=3.95 有区分度
- **3.1 news-sources.json schema_version 2**：四方向 25 信源（openai-blog/the-verge/
  arstechnica/variety 等）；**实测发现并修复失效源**：chinanews scroll.xml 404 → importnews.xml
  （30 条）；huanqiu RSS 停服（301→sitemap）禁用；36kr /feed 返回 SPA HTML 禁用。
- **真实冒烟**（date=当日）：ai-news 32 条 / intl-news 26 条 / cn-news 6 条 / ent-news 2 条，
  采集→打分→fallback selection 全链路通过。

**重要测试教训**：冒烟 `--date` 必须用当日——用未来日期（2026-08-30）时窗口过滤 0 条，
不是 bug：窗口是「前日 08:00 → 当日 08:00」，未来日期的窗口与 RSS 实际发布时间错位。

## 剩余工作清单（下次会话按此继续）

- **Phase 2 剩余（渲染层）**：overview/material/card 新页面类型、tab-nav 全局元素（仿 gprog/footer）、
  字幕条渲染（预渲染 div + opacity fromTo，严禁 tween 改 textContent）、横竖屏自适应规则表
- **Phase 3 剩余**：3.2+ X 采集 scripts/x-watch.ts（twscrape 轮询 x-watchlist.json）、
  3.3 pipeline.json（editions/streams/quota/orientations/subtitle_rules/agent）、
  3.5 剩余 args/run 公共库、3.6 测试设施（node:test + fixtures + parsers.test.ts）、
  3.7 RESEARCH_FIXTURE fixture、3.8 dashboard（/api/source-health + dashboard-add 类型白名单）
- **Phase 4/5 并行支线**（可派子代理）、**Phase 6 进化闭环**、**Phase 7 验证收尾**
- SKILL.md 场次窗口与代码不一致待对齐：SKILL.md 写 morning=昨18:00→今08:00 / evening=08:00→18:00，
  代码 windowForEdition 是 morning=昨08:00→今08:00 / evening=06:00→17:30（Phase 2.6 时统一）

## eduevidence-bilibili 视频整体升级（用户@scene#38「媒体运营」）

**三重问题诊断**
1. 音色不一致：vo-v1-arch.mp3（edge-tts 男声 24k/48k）vs 其余 25 段（CosyVoice2 克隆女声 44.1k/192k）—— batch 重录遗漏，用 edge-tts 兜底。
2. 英文专名读错（Opus/SOL/Claude/Codex 等）—— 参考音频 `prompt_text.txt` 纯中文（whisper 原始转写、繁简混杂+错别字），零英文音素，CosyVoice2 零样本发音空间未覆盖英文。
3. 中文多音字读错 —— CosyVoice2 zero-shot g2p 多音字消歧弱 + 文案零发音规范化。

**关键修正：版本号经过搜索核实**
- 之前轻信「以 vo-m2 为准」改成 GPT-5.5/Kimi K2 是**错的**。真实最新：
  - **GPT-5.6 Sol**（OpenAI，2026-07 GA，三档 Sol/Terra/Luna，官方 API model id gpt-5.6-sol）
  - **Kimi K3**（月之暗面，2026-07 发布并开源，2.8 万亿参数 MoE）
- 全部回滚 + 规范为 `GPT-5.6 Sol` / `Kimi K3` / `GPT-5.6 Terra`（agent.tsx、v1short.tsx、segments_checked.txt 同步修正）

**视觉升级（运镜 + 3D + 转场）**
- 抽象出 `engine/src/kit/motion.tsx`：E 缓动常量、Camera（贯穿全程）、Tilt3D、Parallax
- StickyNote/Stamp 内置 3D（perspective+rotateX/Y+translateZ+spring 浮入），改一处全项目生效
- 顶层 `<Sequence>` 改 `<TransitionSeries>`（slide/fade/clockWipe/iris），「补偿帧」方案保持时间轴与配音挂载点不变
- 修复 render_all.mjs / render_preview.mjs 的 `browserExecutable` 位置 bug（必须是顶层参数，不能塞 chromiumOptions），改用系统 Chrome

**运镜重做（用户说「运镜在哪里？」—— 之前 6% 太小）**
- 搜索开源运镜 skill：在 GitHub 找到 **Vincentwei1021/video-shotcraft**（6.6k stars，「152 shot recipe cards · 209 motion previews · 2.5D camera moves · cinematic product videos with Remotion」）
- 吸收其运镜配方重做 Camera（`engine/src/kit/motion.tsx`）：
  - `pushIn` scale 1.0→1.14 + `Easing.in(quad)` + vignette 暗角（慢推压迫）
  - `pullOut` 1.14→1.0 + `Easing.out(cubic)`
  - `tiltReveal` rotateX -80°→0 + scale 3.2→1（俯仰揭示）
  - `dutchRoll` rotate -10°→0
  - `panL/R` translateX 300px
  - 运镜贯穿整个场景（dur = 场景总帧数 30 基准）
- Root.tsx 各场景分配新运镜 + 传 dur + vignette on pushIn

**配音修复**
- 用户：「先用现在的女声全部克隆」—— 保持 clone_female3.wav 真女声（不换 edge-tts 参考音频）
- `apply_pronunciation.py`：英文专名→中文近音表（EN_MAP：Sol→索尔、Opus→欧普斯、Claude→克劳德、Codex→科戴克斯、DeepSeek→迪普西克、Kimi→奇米 等）+ 数字/百分号→中文数字正则 + `S O L` 拆字母补到索尔
- `regen_audio.py`：读 pronunciations.json 用现有女声 zero-shot 重录全部 27 段，输出 44.1k/192k mp3+wav 到 `engine/public/audio/`，生成 `durations.json`
- 修正 `prompt_text.txt` 简繁+错别字（基于上下文推测，实际内容需 whisper 重核）

**video-shotcraft 关键配方（已吸收）**
- 慢推压迫 C 式：scale [0,120f]→[1.00,1.14] `Easing.in(quad)` + 暗角 opacity 0→0.5 同步
- 拉远孤立 D 式：scale 2.2→0.62/110f `Easing.out(cubic)`，origin 锁主卡
- 多层视差（3 层）：0.35x/0.7x/1.4x，背景 blur 2px+降饱和，前景 blur 3px；无 blur 锚读作"贴片乱飞"
- 俯仰揭示 A 式：rotateX -80°→0 + perspective 600→1200 联动 + scale 3.2→1
- 斜角滚正 B 式：rotate -10°+漂移 → 14f ease-out 冲过 0 到 +1.2° → 10f 收回 0
- 原则：**One shot, one intent**，不要 push+pan+rotate 同时；ease-in-out（`cubic-bezier(0.65, 0, 0.35, 1)` 或 `bezier(0.4, 0, 0.2, 1)`）；Slower = more cinematic（800-2000ms hero move）

**待办**
- 配音重录跑完后，用 whisper ASR 转写验证发音（特别 Opus/Sol/Claude/多音字）
- 配音重录后更新 Root.tsx 音频驱动时间轴（消费 durations.json）
- 正式渲染三 composition（v1h/v1v/v2）+ 抽帧对比 + 音画同步检查
- Qwen3-VL-Embedding 视频语义理解模块（用户最后需求）


## 视觉形式重构：卡片 PPT → 手绘动画（晚间定稿）

**用户核心诉求（多次纠正后最终明确）**：
- 视频不能是"卡片 PPT"（便利贴/徽章/印章+入场动画），要**真正的动画**
- 手绘动画 + 抽象比喻 + 具体架构图 + 运镜，2D/2.5D 即可
- 背景暖米黄纸张（不纯白刺眼）；**小猫进度条 ProgressCat 必须保留**
- 手绘顺序不能错（按叙事语义 sequence+narrativeRole）
- 整合三个开源仓库 + 做自己的风格创新；范围先 V1 短版（110s/11 幕）

**三个方法论源（已研究）**：
1. `geeklee/srt-whiteboard-animation`(2572★)：暖米黄#F6F1E3 + 流式笔迹（ink线稿→color添彩→gaze凝视）+ mask 分区遮罩（annotation.json：sequence/narrativeRole/region/reveal/handPath），叙事顺序=场景铺垫→关键人物→动作冲突→反应结果。已在本地 `~/.workbuddy/skills/`
2. `geeklee/srt-vox-director`(72★)：Vox 风格解释视频分镜（21 风格库，抽象比喻，4/6/8/10s 吸附）
3. `cathrynlavery/diagram-design`(28522★)：**已拉取安装** `~/.workbuddy/skills/diagram-design`（582行 SKILL + 53 references + 38 种编辑级图表：Layer stack/Process/Dependency graph/Flowchart/Sankey/Architecture/Quadrant…，支持 sketchy/hand-drawn 风格，哲学"最高质量的动作是删除"）

**白板渲染环境**：Python310（`C:/Users/10777/AppData/Local/Programs/Python/Python310/python.exe`）有 cv2 5.0 + numpy 1.26.4。白板 skill 有 `prepare_env.py` 可建 .venv。

**V1 已完成**：
- `gen_v1_srt.py` 生成 `assets/whiteboard/v1/v1.srt`（10 幕，VO_V1S 挂载点 + durations.json）
- 第 1 幕 IntroS 线稿 `scene-01-intro.png`（ImageGen，暖米黄手绘 AI 机器人+书桌+书堆）+ `scene-01-intro.annotation.json`（3 分区叙事顺序：桌面铺垫→机器人主体→书堆结果）+ `render_annotation_preview.py` 预览 OK
- `render_stream_whiteboard.py` 渲染 `scene-01-intro-whiteboard.mp4`（1080x720/60fps/9.1s）成功，抽帧验证手部逐区绘制正确
- IntroS 横版场景已重写为 OffthreadVideo 白板 MP4 + 文字锚点叠加（标题 EduEvidence/先摆证据再下判决），`engine/public/whiteboard/v1/` 已建

**下一步**：其余 9 幕线稿+annotation+渲染 → Remotion 整合 → 全量渲染 V1H → 同步 skill 工作流（新建项目编排 skill，整合三源 + 小猫/配色/文字叠加）

## 视觉形式重构 v5：架构图标准答案 + 即梦图生图手绘

**用户三连纠正（关键拐点）**：
1. **颜色一致**：geeklee 演示画完与最终同色；我们"画完变淡"根因 = 原图线条太浅（淡铅笔素描），ink 揭示是二值化黑墨迹，color 还原回浅淡原图反差大。**修：生图浓黑炭笔+高对比**。
2. **内容转型（最重要）**："我介绍 GitHub skill 不是讲故事，画图也是画技术，偶尔比喻图"——**线稿从故事插画（AI机器人+书堆）全部改为技术图表**（diagram-design 编辑级规范：Layer stack / Process / Dependency graph / Flowchart / Sankey / Architecture / Quadrant）。
3. **文字双轨**：线稿内手绘标注（突破 geeklee"禁文字"惯例，ink 揭示把文字当墨迹画出来）+ Remotion 动态强调（Typewriter/CountUp/HandUnderline）。
4. **运镜**：保留 Camera 层。
5. **观众语义修正（必守）**：**提问的是"人"（教师/学生），不是"宿主"**——架构里不出现"宿主接入"，统一说"装进你的AI助手"。视频口吻"你提问"。

**产品真源**：
- GitHub `37chengshan/eduevidence`（README + 视频项目根 `architecture.json` schema v1）
- AI Agent Skill 装进 Claude Code/Codex/OMP；9 步证据流+8 智能体+4 态判决+5 报告+3 图表

**生图渠道切换（aiping）**：
- base_url `https://aiping.cn/api/v1`，key 存 `D:/motion/.secrets/aiping_key.txt`（已 gitignore，**key 已在对话暴露，建议后台轮换**）
- 模型「**即梦图片生成 4.0**」（中文文字准，0元/张），通过 `image` 图生图保结构换风格
- `D:/motion/tools/imagegen/gen_lineart.py`（urllib+JSON，已扩展 `--image` 自动转 base64 data URI）

**关键流程（v5 确立）**：
1. **标准答案图先行**：`D:/motion/projects/eduevidence-bilibili/tools/render_architecture.py`（PIL，无新依赖）读 `architecture.json` + diagram-design `type-architecture.md` 规范渲染编辑级架构图 `arch-reference.png`（1536x1024 暖米黄，4 分区+正交 elbow+焦点节点）
2. **图生图转手绘**：标准图作 image 喂即梦 4.0，提示"转浓黑炭笔白板风格，保留全部节点/中文/箭头不变" → `arch-handdrawn.png`（结构/文字/方向 100% 保留）
3. **白板 skill 渲染**：annotation（3 分区：入口/核心/产出，sequence 按观众阅读顺序）→ `render_stream_whiteboard.py` → MP4
4. **Remotion 整合**：OffthreadVideo + 文字锚点叠加（Typewriter/CountUp/HandUnderline）+ Camera 运镜 + ProgressCat

**第 1 幕 IntroS 验证（v5）**：
- 标准答案图清晰：4 分区（你的AI助手/EduEvidence核心/智能体与模型/产出）、12 节点、焦点红点（你/9步/4态判决）、正交连线+纸色标签
- 即梦图生图手绘效果：浓黑炭笔线条、暖米黄白板、做旧质感、四色点缀、手部"江哥是老登啊"
- annotation 3 分区：入口(0-2.2s) → 核心(2.6-5.0s) → 产出(5.4-7.8s) + gaze(7.8-9.1s)
- 白板 MP4 渲染成功（1.68MB），抽帧验证叙事顺序
- IntroS 横版场景已重写（v3 改版）→ 现覆盖 v5 新 MP4，无需改组件代码

**关键文件**：
- 工具：`D:/motion/tools/imagegen/gen_lineart.py`、`D:/motion/projects/eduevidence-bilibili/tools/render_architecture.py`
- 资产：`D:/motion/projects/eduevidence-bilibili/assets/whiteboard/v1/{arch-reference.png, arch-handdrawn.png, scene-01-intro.annotation.json, scene-01-intro-whiteboard.mp4}`
- 引擎：`D:/motion/projects/eduevidence-bilibili/engine/src/scenes/v1short.tsx`（IntroS 改 OffthreadVideo）、`engine/src/Root.tsx`（ProgressCat 保留）
- Skill：`~/.workbuddy/skills/{srt-whiteboard-animation, diagram-design, srt-vox-director}/`

**待做**：
- IntroS 整合预览（v5 新 MP4 后）→ 用户确认
- 批量剩余 9 幕（每幕：标准图→图生图→annotation→白板 MP4→重写场景组件）
- 全量渲染 V1H 110s + anti-ppt-gate 抽帧
- 同步 skill：新建项目编排 skill（整合三源 + 即梦生图 + 小猫/四色/双轨文字）


## v6：批量 9 幕白板技术图 + Seedream 切换

- **Seedream-4.5 上游 400 不可用**（所有 size/prompt 均 400）→ 改用同系列 **Doubao-Seedream-4.0**（image2image，图生图），第 1 幕验证：文字清晰/结构 100% 保留/浓黑炭笔，**远超即梦 4.0**
- 9 幕标准技术图：`tools/render_scenes.py`（PIL，观众语言无内部术语）→ arch-02~10 标准图（Layer stack/Process/Dependency graph/Flowchart/Architecture/Sankey/Quadrant/终端）
- 图生图：arch-03 成功 handdrawn，其余 7 张 Seedream-4.0 间歇 image_count:0（内容过滤/不稳定）→ **降级用标准图直接作白板线稿**（文字清晰结构正确）
- 白板批量：`tools/batch_render_whiteboard.py`（8 幕 annotation 3 分区叙事顺序 + render_stream_whiteboard.py）→ scene-02~10-whiteboard.mp4 全部成功
- 场景组件：v1short.tsx 加通用 `WhiteboardLayer`（暖米黄+OffthreadVideo 白板 MP4+文字锚点 Kicker/Badge/Title/Subtitle/Cta），7 幕横版由 `tools/wb_patch.py` 批量替换为 WhiteboardLayer 调用（注意 title 需 JSON 字符串或 JSX 片段包裹）；IntroS/ArchS 单独改版
- 编译 BUNDLE_OK；完整 V1H（110.96s）渲染中

## v7：最终生图路线 = WorkBuddy ImageGen（用户定案）

- 用户："停停停你还是直接调用workboddy自带的生图吧 为什么要降级呢"——**aiping/Seedream 全弃**（Seedream-4.5 上游 400 不可用；Seedream-4.0 图生图把架构图当自由涂鸦，涂改严重："花"）
- **最终路线：WorkBuddy ImageGen 文生图**（5-10 积分/张），提示词描述结构（观众语言+中文标注），一次出图文字准确/结构清晰/手绘白板风，远超 aiping
- 9 幕 wb 图：arch-01-intro ~ arch-10-ctas（-wb.png），ImageGen footnote="" 仍带"AI生成 WORKBUDDY"水印 → **PIL 覆盖右下 290x90 纯色 #F5EBD7** 去水印
- 重渲 8 幕白板（rerender_8.py 子进程；batch import 模式会 SIGTERM，须独立 subprocess.run）+ scene-01/10 手动补渲
- 全部 9 幕白板 MP4（wb 图）→ engine/public/whiteboard/v1/
- WebTourS 保留录屏（landing-user60.mp4）
- 完整 V1H（110.96s）渲染中；**C 盘 99% 满，Remotion 渲染必须 TMPDIR=D:/tmp TEMP=D:/tmp**

## v8/v9：恢复第一版 + 用户 4 问题修复 + 配音重合成（今日定稿）

**用户最终定案**："算了恢复第一版就是现在存的三种，只是优化排版和内容（观众如何使用视角）+ 所有框居中放大 + 聚焦运镜 + 声音重新合成（读音问题）"

**已做**：
1. **恢复第一版**：v1short.tsx 重建为第一版卡片 PPT 风（10 幕，无手绘/生图/白板），框居中放大 + 观众"你提问→判决书"视角；手绘版备份 v1short.wb-handdrawn.bak
2. **聚焦运镜**：kit/motion.tsx Camera 加 `focusIn`（focus 点 + translate 补偿使焦点居中放大）；**先全览后聚焦**（前 55% 静止展示全览，后 45% ease-in-out 缓慢推近——establishing shot 经验）；不需要运镜的幕 cam:"none"（ArchS/AgentS/McpS/CaseS/ReportCardsS/WebTourS 固定）
3. **文字高频抖动**：排查 Float(低频正弦)/ProgressCat(低频)/Stamp(低频) 均非高频；最可能 Camera transform 每帧变化+Chrome 文字重光栅化 → focusIn 前 55% 静止 + 其他幕 none 已缓解
4. **WebTourS**：去运镜(none) + 录屏提亮 filter brightness(1.2) contrast(1.05)
5. **McpS 深度重做**：智能体 → Agent MCP 协议桥（STDIO/SSE/HTTP + 可审计 JSON）→ 多底座三层结构 + 跨基座交叉验证
6. **配音重合成（CosyVoice）**：regen_audio.py 语速 1.2→**1.1** + ffmpeg loudnorm -14 LUFS + afade 0.1s 首尾；27 段重合成完成；**读音纠正沿用 apply_pronunciation.py 的 EN_MAP**（E D U Evidence/迪普西克/索尔 等中文近音 + 数字转中文）；CosyVoice 读音经验：中文多音字 [h][ǎo] 拼音标注、英文 ARPAbet 音素标注（[K][OW1]...）
7. **时间轴重算**：语速 1.1 后配音时长 +8~14%（vo-v1-01: 10.18s 等）→ update_timeline.py 重算 V1S_SEG/V1V_SEG start/end + VO_V1S 挂载 + total30（总时长 110.96→**114.36s**）；V2 时间轴待重算（VO_V2/V2_SEG）
8. **运镜经验（搜到）**：推镜头=从全景到特写（establishing→focus）；固定镜头适合展示/静物；One shot one intent（一次一个意图，别推+移+转同用）；ease-in-out

**关键文件**：v1short.tsx（重建）/ Root.tsx（时间轴+focusIn）/ kit/motion.tsx（focusIn）/ regen_audio.py（语速1.1+音量+淡入淡出）/ update_timeline.py（时间轴重算）

**待做**：全量渲染新 V1H 验证 → V2 时间轴重算 → 三 composition 渲染
