# 2026-09-04

## motion-video 总视频工作流 skill 建库（Phase A，plan cosmic-nebula-babbage 已批）

### 背景
用户要整合项目为视频工作流 skill：入口→方向→工作流→文案人工审查→初稿→审查3轮→出片→发布(Windows)；
总 SKILL.md 只做目录；覆盖三方向；新闻搜索不搞坏；Qwen 向量库纳入离线底座；skill 可持续进化；顶层目录重构。

### 三个方向（用户定）
1. 每日新闻（ai-news 重点/intl/cn/ent 四流，真相源 producer/src/lib/streams.ts）
2. GitHub 热点/个人项目介绍（github-daily + EduEvidence/archify）
3. 实操视频/录屏介绍（video-talkcraft 全流程，落地 vtc/ 工程：79 卡已复制+字级时间戳跑通）

### 调研关键结论（3 Explore agent + 4 WebSearch 产物）
- **新闻链路**：daily-research→score-and-rank→generate-content(DeepSeek@aiping)→media-orchestrate→prepare-audio/gen-srt→BlockRenderer→publish。
  generate-content --stream 半迁移（只认 ai-news|world-news）。不能破坏清单 10 条（X profile/CDP 9223/微博 uid 缓存/news-sources/AIHOT 匿名/env/…）。
- **agy = Antigravity CLI**（Google Gemini 终端 Agent）：review-video.ts 调 `agy --model gemini-3.7-flash-* --print @视频`，
  Google OAuth 登录态（~/.gemini/antigravity-cli/），失败无人工兜底（available() 恒 true → exit(1)）。
  用户指定升级 gemini 3.8 + 加"失败导出 prompt 给用户人工审查"兜底（执行阶段改 review-video.ts）。
- **向量库**：embed-server 四件套孤儿 CLI（未接主流水线）；2048 维跨模态；视频 ~34s/条单实例独占 GPU → 离线底座；
  两落点：A 选题去重 news-archive（文本，便宜先接）B 片段复用 video-segments。
- **autosearch**（0xmariowu/Autosearch + clusterclaw experiment loop）自我迭代机制：session.jsonl append-only +
  LIVING.md living doc + reflection block + post-mortem 回写 patterns + 指标改善才保留。
- **排版模板现状**：streams.ts 四流无视觉映射（每方向排版模板空白）——素材齐全：三套 token（Apple/手帐 C/报纸+dark-tech）+ BlockRenderer 9 类型 + 79 卡 + 字幕规范冲突待统一。

### 产物：motion-video skill（双位置各 30 文件）
- SKILL.md 75 行目录型（三方向路由表 + 总流程 + doc/ 索引 + scripts 命令 + 引用清单）
- doc/ 12 篇：00 总流程(统一红线) / 01 每日新闻(不能破坏清单) / 02 GitHub个人项目 / 03 实操录屏 /
  04 文案人工审查 / 05 审查3轮(机器闸+subagent+agy终审+失败兜底) / 06 发布(sau) / 07 向量库 /
  08 目录约定 / 09 排版模板(方向排版卡) / 10 迭代进化(autosearch) / LIVING.md
- scripts/ 16 个机器闸（复制自 video-talkcraft，自包含无内部引用）
- references/README.md 引用映射
- 路径：D:/motion/.workbuddy/skills/motion-video/ + C:/Users/10777/.workbuddy/skills/motion-video/（checksum 一致）

### 关键事实（Phase B 前置）
- **D:/motion 顶层是单一 git 仓库**（rev-parse true，无子仓库）→ 顶层 git mv 可回滚。
- 待办缺口：generate-content 四流；vector frames 统一(4 vs 3)；dark-tech 皮肤；
  publisher(Mac) 废弃；review-video.ts 升级 gemini 3.8 + 人工兜底。

### 待办
- Phase B：顶层重构（外侧 project/ + doc/ + code/ + data/ + models/，每项目 日期-名称）——大破坏性操作，
  需分批 git mv + 全仓硬编码更新 + 回归（news/embed-server/render），每批一个 commit。
- Phase C：回归验证。
