# 2026-09-03 工作日志

## 今日新闻 → video-talkcraft 视频成片
- 跑通项目自有新闻管线取**今天**新闻：`producer/scripts/daily-research.ts --date 2026-09-03 --stream ai-news --edition morning`
  → 34 条；`score-and-rank.ts` 出 top.md。选 **Meta Muse Spark 1.3**（X @kimmonismus / @alexandr_wang：max 版 Artificial Analysis 智能指数 62 分、xhigh 61 分、五个月第 4 版、逼近 Claude/GPT-5.6）。
- 用 **video-talkcraft** skill（已装 `~/.workbuddy/skills/`，用户定版"装 skill 先搜 GitHub"）跑口播视频：
  - 口播稿 `vtc/work/script.json`（13 句、数字汉字、首句钩子 62）。
  - 配音：**Fun-CosyVoice3-0.5B**（`tools/voice/cosyvoice/synth_news.py`，clone_female3.wav，PROMPT3 前缀，speed 1.2）→ `vtc/work/news_vo.wav`（56.86s）。
  - 字级时间戳：HF 代理把 faster-whisper 模型下成 0 字节（仅 model.bin 经 hf-mirror curl 走通）。
    **改用逐句合成记录精确起止**（`segments.json` + `build_timing.py` → `timing.json`，schema 同 make_timing.py），免去 ASR 下载。
  - Remotion 工程 `D:/motion/vtc`：暗色 Apple-tech 背景（呼吸 idle + 对角扫光）+ 词级字幕（skill `Subtitles` 组件，词锚驱动）+ hero "62" 在"六十二"词锚处显形（未到拍不显形）。
  - 渲染：`npx remotion render src/entry.tsx NewsVideo out/news_v1.mp4 --browser-executable <chrome-headless-shell> --codec h264` → 1796 帧；loudnorm 交付 `out/news_v1_delivery.mp4`（60.0s, 9.75MB）。

## 环境坑（已踩平，可复用）
- **Remotion 4.0.518 Windows 渲染**：① `@remotion/compositor-win32-x64-msvc` 原生二进制被代理截断 → 从 `producer/node_modules/@remotion/compositor-win32-x64-msvc/` 整目录拷到 `vtc/node_modules/...`；② chrome 用 `D:/motion/.remotion/chrome-headless-shell/win64/chrome-headless-shell-win64/chrome-headless-shell.exe`，**路径必须用 Windows 盘符 `D:/...` 而非 `/d/...`**（Git Bash 不翻译给 node）；③ `Easing.backOut` 在 4.0.518 不存在 → 用 `Easing.bezier(0.34,1.56,0.64,1)`。
- **HF/代理**：`huggingface_hub` 与 `remotion browser ensure` 经 127.0.0.1:7890 都失败；大文件偶有走通（hf-mirror curl），小文件schannel 握手失败。
- **CosyVoice3 数字读法**："GPT-5.6" 被读成"GPT 负五点六"（连字符当减号）→ 口播稿改空格 "GPT 5.6"。短句会触发"too short than prompt text"警告但会用正确文本重合成，结果正确。
- **Subtitles 组件**：keyword-pop 全片上限 **3** 次，且同一词在多个句子出现会重复计数 → 关键词需去重且不跨句重叠。

## v2/v3：用户反馈“就一个新闻？视频也只有一个画面？”→ 5 镜重构 + 机器闸通过
- 用户不满 v1 单画面。按 video-talkcraft 反 PPT 七层系统，将 `NewsVideo.tsx` 重构为 **5 个独立镜头**：
  1. **钩子**（s1-s2）：标题 Muse Spark 1.3 + 62 分卡；
  2. **指数对比**（s3-s4）：条形图 62 vs Claude/GPT-5.6 第一梯队虚线；
  3. **版本节奏**（s5-s6）：v1.0→v1.1→v1.2→v1.3 链；
  4. **能力升级**（s7-s10）：智能体 Agentic / 科学推理 Scientific 两枚 pill；
  5. **收尾**（s11-s13）：62 分 / 4 版本 / 5 个月 chips。
- 每镜 soft-reveal 进场 + 镜头微推（1.0→1.045）+ 全局环境 idle，转场在语义拍边界。
- 输出：横屏 `news_v3_delivery.mp4`（1920×1080, 60s, 22MB）+ 竖屏 `news_v3_vertical_delivery.mp4`（1080×1920, 60s, 22MB）。
- **机器闸 `motion_check.py`**：
  - 初版 v2 因元素驻留时背景 idle 太弱，被 `freezedetect` 抓出 26-29 段 ≥0.8s 静止 → FAIL。
  - v3 加强全局环境层：**缓慢平移网格 + 双对角扫光 + 24 颗漂浮微粒 + 呼吸暗角 + 极淡噪点层** → `[静止] PASS`（noise=0.003）。
  - 抖动闸：横屏默认 crop `1200:120:150:150` 通过；竖屏 1080 宽放不下该 crop，需手动传 `--window t,900:120:90:150` → 也通过。
  - 运行环境：`numpy` + `imageio` 需装在执行 `python` 对应的解释器里；项目用 managed venv `C:/Users/10777/.workbuddy/binaries/python/envs/default`。
