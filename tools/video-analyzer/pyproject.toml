[project]
name = "video-analyzer"
version = "0.1.0"
description = "本地视频下载分析工具（Tina 逆向方法论复现入口）：va download/ingest/search/analyze"
requires-python = ">=3.10"
dependencies = [
    "yt-dlp>=2025.1.1",
    "httpx>=0.27",
]

[project.scripts]
va = "va.cli:main"

[tool.uv]
package = true
